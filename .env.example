# MongoDB Connection (optional — playground uses in-memory mock by default)
# MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/myFirstDatabase

# App
# NEXT_PUBLIC_APP_URL=http://localhost:3000
